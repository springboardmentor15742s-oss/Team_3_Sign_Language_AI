server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routing — unknown paths (e.g. a refresh
    # on /dashboard) must fall back to index.html instead of 404ing.
    location / {
        try_files $uri /index.html;
    }
}
