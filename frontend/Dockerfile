# Sign Language Learning & Assessment Platform — Frontend (React + Vite)
# Multi-stage build: compile with Node, serve the static output with nginx.

FROM node:20-slim AS build
WORKDIR /app

# Lets the backend URL be baked in at build time for a given deployment
# (defaults to the docker-compose service name so the two containers can
# talk to each other on the compose network).
ARG VITE_API_BASE_URL=http://localhost:8000
ENV VITE_API_BASE_URL=${VITE_API_BASE_URL}

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build

FROM nginx:1.27-alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
